from tkinter import Tk
from core.gui import Geodetic_MB_GUI

if __name__ == "__main__":
    root = Tk()
    app = Geodetic_MB_GUI(root)
    root.mainloop()

def main():
    root = Tk()
    app = Geodetic_MB_GUI(root)
    root.mainloop()
